## 功能：
1、对Cloudflare的IP段进行测速；
2、生成延迟最低的20个IP；
3、生成的IP后面增加#国家国旗及延迟数据；
4、输出结果放在指定目录下；
5、每天固定时间段进行测速。
## 前提：
1、下载好Cloudflare给的所有IP段，生成一个CFIPV4.txt文件；
2、下载GeoLite2-Country.mmdb，用于识别国家。
## 部署：
将这个应用部署到威联通NAS中，测速的结果比较真实。
